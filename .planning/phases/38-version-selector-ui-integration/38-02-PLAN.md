---
phase: 38-version-selector-ui-integration
plan: 02
type: execute
wave: 2
depends_on: [38-01]
files_modified:
  - src/components/experience/__tests__/version-regression.test.tsx
autonomous: true
requirements: [VER-03]

must_haves:
  truths:
    - "V1 experience functions identically to pre-Phase-38 behavior"
    - "V1 default selection means no behavioral change unless operator explicitly chooses V2"
    - "OracleExperience with V1 version passes all voice routing through unchanged V1 path"
    - "VersionSelector defaults to V1 (matching VersionProvider initialVersion default)"
  artifacts:
    - path: "src/components/experience/__tests__/version-regression.test.tsx"
      provides: "V1 regression and V2 routing verification tests"
      min_lines: 40
  key_links:
    - from: "src/components/experience/__tests__/version-regression.test.tsx"
      to: "src/contexts/VersionContext.tsx"
      via: "VersionProvider wrapping test components"
      pattern: "VersionProvider"
    - from: "src/components/experience/__tests__/version-regression.test.tsx"
      to: "src/components/experience/VersionSelector.tsx"
      via: "Render and interact with selector"
      pattern: "VersionSelector"
---

<objective>
Write regression tests verifying V1 functions identically after Phase 38 changes and V2 selection flows correctly.

Purpose: Satisfies VER-03 -- V1 must function exactly as today with zero regression. The version selector defaults to V1 and the entire TTS pipeline (already wired in Phase 37) routes V1 audio identically to pre-dual-voice behavior.

Output: Regression test file verifying V1 default, version persistence, and selector integration
</objective>

<execution_context>
@C:/Users/USER/Oraculo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/USER/Oraculo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/USER/Oraculo/.planning/ROADMAP.md
@C:/Users/USER/Oraculo/.planning/STATE.md
@C:/Users/USER/Oraculo/.planning/REQUIREMENTS.md
@C:/Users/USER/Oraculo/.planning/phases/38-version-selector-ui-integration/38-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/contexts/VersionContext.tsx:
```typescript
export function VersionProvider({ children, initialVersion = 'V1' }: VersionProviderProps): JSX.Element;
export function useVersion(): { version: ExperienceVersion; setVersion: (v: ExperienceVersion) => void };
```

From src/types/index.ts:
```typescript
export type ExperienceVersion = 'V1' | 'V2';
```

From src/components/experience/VersionSelector.tsx (created in Plan 38-01):
```typescript
interface VersionSelectorProps {
  onContinue: () => void;
}
export default function VersionSelector({ onContinue }: VersionSelectorProps): JSX.Element;
// data-testid: version-selector, version-v1, version-v2, version-continue
```

From src/lib/voice/voiceClassification.ts:
```typescript
export function getVoiceType(key: string): VoiceType;
// Returns 'VOZ_PERGUNTA' for *_PERGUNTA, FALLBACK_*, TIMEOUT_*, APRESENTACAO, ENCERRAMENTO
// Returns 'VOZ_NARRATIVA' for everything else
```

From src/services/tts/fallback.ts:
```typescript
// V1 loads from /audio/prerecorded/{key}.mp3
// V2 narrative loads from /audio/prerecorded/v2/{key}.mp3
// V2 question loads from /audio/prerecorded/{key}.mp3 (same as V1)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write V1 regression and version integration tests</name>
  <files>src/components/experience/__tests__/version-regression.test.tsx</files>
  <read_first>
    - src/contexts/VersionContext.tsx (VersionProvider interface and defaults)
    - src/contexts/__tests__/VersionContext.test.tsx (existing test patterns -- wrapper function, renderHook, act)
    - src/components/experience/VersionSelector.tsx (component created in Plan 38-01 -- data-testids, behavior)
    - src/components/experience/OracleExperience.tsx (lines 267-281 for state variables, lines 641-650 for render flow)
    - src/lib/voice/voiceClassification.ts (getVoiceType function)
    - src/services/tts/fallback.ts (URL resolution logic for V1 vs V2)
  </read_first>
  <action>
Create `src/components/experience/__tests__/version-regression.test.tsx` with the following test suites:

**Suite 1: "V1 regression -- zero behavioral change (VER-03)"**

Tests that verify V1 is the unchanged default path:

1. **"VersionProvider defaults to V1"** -- `renderHook(() => useVersion(), { wrapper: VersionProvider })` returns `version === 'V1'`. This is a contract test ensuring the default was not changed.

2. **"VersionSelector renders with V1 pre-selected"** -- Render `<VersionProvider><VersionSelector onContinue={vi.fn()} /></VersionProvider>`, verify the V1 button (`data-testid="version-v1"`) has the selected visual state (check for `aria-pressed="true"` or verify text opacity class). V2 button should NOT be selected.

3. **"V1 selection does not call setVersion unnecessarily"** -- Render VersionSelector within VersionProvider, click the V1 button (already selected). Since V1 is the default, clicking it should not cause a version change. Version should still be V1 after clicking.

4. **"Continuar with V1 default proceeds without changing version"** -- Render VersionSelector, click Continuar without clicking V2. Verify `onContinue` is called AND version is still 'V1'.

5. **"V1 voice classification routes all keys to single voice path"** -- Import `getVoiceType` and verify that for V1, the voice type classification still works (APRESENTACAO -> VOZ_PERGUNTA, INFERNO_Q1_SETUP -> VOZ_NARRATIVA). This is a contract test ensuring Phase 36 classification was not broken. In V1, the voice type is derived but the version='V1' means only one voice ID is used server-side.

**Suite 2: "V2 selection flow"**

Tests that verify V2 can be selected and persists:

6. **"Selecting V2 updates version context"** -- Render VersionSelector in VersionProvider, click V2 button, verify version changes to 'V2'.

7. **"V2 selection persists after clicking Continuar"** -- Select V2, click Continuar, re-read version from hook -- still 'V2'.

8. **"Version persists across re-renders (session persistence VER-02)"** -- Set version to V2 via setVersion, rerender, verify still V2. This re-confirms VER-02 from Phase 36 still holds after Phase 38 changes.

**Test utilities:**
- Import `{ render, screen, fireEvent }` from `@testing-library/react`
- Import `{ renderHook, act }` from `@testing-library/react`
- Import `{ describe, it, expect, vi }` from `vitest`
- Import `{ VersionProvider, useVersion }` from `@/contexts/VersionContext`
- Import `VersionSelector` from `../VersionSelector`
- Import `{ getVoiceType }` from `@/lib/voice/voiceClassification`
- Create wrapper: `function wrapper({ children }: { children: ReactNode }) { return <VersionProvider>{children}</VersionProvider>; }`
  </action>
  <verify>
    <automated>cd C:/Users/USER/Oraculo && npx vitest run src/components/experience/__tests__/version-regression.test.tsx --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/components/experience/__tests__/version-regression.test.tsx contains `describe('V1 regression`
    - src/components/experience/__tests__/version-regression.test.tsx contains `VER-03`
    - src/components/experience/__tests__/version-regression.test.tsx contains `VersionProvider`
    - src/components/experience/__tests__/version-regression.test.tsx contains `VersionSelector`
    - src/components/experience/__tests__/version-regression.test.tsx contains `getVoiceType`
    - src/components/experience/__tests__/version-regression.test.tsx contains at least 6 `it(` calls
    - src/components/experience/__tests__/version-regression.test.tsx exits 0
  </acceptance_criteria>
  <done>8 tests pass covering V1 regression (VER-03) and V2 selection flow. V1 default is verified unchanged, V2 selection persists, voice classification contracts confirmed intact.</done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite to confirm zero regression</name>
  <files></files>
  <read_first>
    - src/components/experience/OracleExperience.tsx (verify VersionSelector import and rendering changes from Plan 38-01)
  </read_first>
  <action>
Run the full test suite to verify zero regression across the entire codebase after Phase 38 changes:

```bash
cd C:/Users/USER/Oraculo && npx vitest run --reporter=verbose 2>&1 | tail -50
```

Expected outcome: All previously passing tests (~733 baseline) continue to pass. The only new tests are:
- `src/components/experience/__tests__/VersionSelector.test.tsx` (5 tests from Plan 38-01)
- `src/components/experience/__tests__/version-regression.test.tsx` (8 tests from this plan)

If any pre-existing test fails, investigate whether the failure is caused by Phase 38 changes (VersionSelector import, versionSelected state, render flow change in OracleExperience). If so, fix the regression. If the failure is pre-existing (known: 2 test files reference obsolete PURGATORIO_A/B states), document as pre-existing.

The key V1 regression check: OracleExperience still renders PermissionScreen, then (now) VersionSelector, then StartButton. The machine flow (IDLE -> START -> APRESENTACAO -> ...) is unchanged. The TTS routing with version='V1' produces identical behavior to pre-Phase-38.
  </action>
  <verify>
    <automated>cd C:/Users/USER/Oraculo && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Full test suite output shows 0 new failures beyond the known pre-existing baseline (~16 failing)
    - vitest run exit code is consistent with pre-Phase-38 baseline (some known failures exist)
    - No test file that was previously passing now fails
  </acceptance_criteria>
  <done>Full test suite confirms zero regression from Phase 38 changes. All pre-existing tests pass. New tests (VersionSelector + version-regression) pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan is test-only -- no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-03 | I (Information Disclosure) | version-regression tests | accept | Tests run locally only, no sensitive data. Test file contains no secrets. |
</threat_model>

<verification>
1. version-regression.test.tsx has 8+ tests covering V1 default, V2 selection, persistence
2. All 8 regression tests pass
3. Full test suite shows zero new failures from Phase 38 changes
4. V1 default behavior is verified by explicit tests
</verification>

<success_criteria>
- V1 regression tests explicitly verify zero behavioral change (VER-03)
- V2 selection flow tests verify version switching and persistence
- Full test suite passes with no new regressions
</success_criteria>

<output>
After completion, create `.planning/phases/38-version-selector-ui-integration/38-02-SUMMARY.md`
</output>
