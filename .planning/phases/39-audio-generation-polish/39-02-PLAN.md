---
phase: 39-audio-generation-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 39-01
files_modified:
  - public/audio/prerecorded/v2/*.mp3
  - public/audio/prerecorded/encerramento.mp3
autonomous: false
requirements:
  - AUD-02
  - AUD-03

must_haves:
  truths:
    - "47 narrative MP3 files exist in public/audio/prerecorded/v2/ directory"
    - "ENCERRAMENTO MP3 in public/audio/prerecorded/ says 'Faca' not 'Faz'"
    - "V2 MP3 filenames match lowercase script keys (e.g., inferno_intro.mp3)"
    - "FallbackTTS serves correct paths for V2 narrative segments"
    - "Total MP3 count is 129 (82 V1 + 47 V2)"
  artifacts:
    - path: "public/audio/prerecorded/v2/"
      provides: "47 V2 narrative MP3 files with somber voice"
    - path: "public/audio/prerecorded/encerramento.mp3"
      provides: "Regenerated ENCERRAMENTO with 'Faca' text"
  key_links:
    - from: "src/services/tts/fallback.ts"
      to: "public/audio/prerecorded/v2/"
      via: "getPrerecordedUrl returns /audio/prerecorded/v2/{key}.mp3 for V2 narrative"
      pattern: "/audio/prerecorded/v2/"
    - from: "public/audio/prerecorded/v2/"
      to: "scripts/generate-audio-v3.ts"
      via: "Generated by --v2 mode"
      pattern: "prerecorded/v2"
---

<objective>
Generate all V2 narrative MP3s and regenerate ENCERRAMENTO MP3 with the "faca" text fix.

Purpose: This is the actual MP3 generation step that produces the audio assets for the V2 dual-voice experience. It generates 47 narrative segments using the somber voice (ELEVENLABS_VOICE_ID_V2) and regenerates the ENCERRAMENTO MP3 with the corrected "Faca" text for V1.

Output: 47 new MP3 files in public/audio/prerecorded/v2/, 1 regenerated encerramento.mp3 in V1 directory
</objective>

<execution_context>
@C:\Users\USER\Oraculo\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\USER\Oraculo\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\USER\Oraculo\.planning\PROJECT.md
@C:\Users\USER\Oraculo\.planning\ROADMAP.md
@C:\Users\USER\Oraculo\.planning\STATE.md
@C:\Users\USER\Oraculo\.planning\phases\39-audio-generation-polish\39-RESEARCH.md
@C:\Users\USER\Oraculo\.planning\phases\39-audio-generation-polish\39-01-SUMMARY.md

<interfaces>
<!-- Key facts for executor -->

Generation script commands (after Plan 39-01):
- V2 narrative generation: `npx tsx scripts/generate-audio-v3.ts --v2`
- V1 ENCERRAMENTO regen: `npx tsx scripts/generate-audio-v3.ts --force` (will skip all existing except regenerate all, OR use specific key filter if available)
- Dry-run check: `npx tsx scripts/generate-audio-v3.ts --v2 --dry-run`

Expected V2 output: 47 MP3 files in public/audio/prerecorded/v2/
Expected V1 regen: 1 file (encerramento.mp3) in public/audio/prerecorded/

ENCERRAMENTO is VOZ_PERGUNTA — it does NOT get a V2 MP3 because in V2 mode, VOZ_PERGUNTA segments use the V1 voice. The V1 encerramento.mp3 is shared between V1 and V2. Therefore only V1 regeneration with --force is needed.

Env vars needed (in .env.local, fixed by Plan 39-01):
- ELEVENLABS_API_KEY=sk_49336...
- ELEVENLABS_VOICE_ID=PznTnBc8X6pvixs9UkQm (V1 Oracle voice)
- ELEVENLABS_VOICE_ID_V2=GIuLCSVfgJaUuh7hYOY8 (V2 somber voice)

Rate limiting: 1s delay between requests (built into script). Resume-safe (skips existing files).
Estimated cost: ~7,100 characters total, ~7% of Creator plan monthly quota.
Estimated time: ~2-3 minutes for 47 files (1s delay each + API latency).

FallbackTTS path resolution (from src/services/tts/fallback.ts):
```typescript
private getPrerecordedUrl(key, version = 'V1', voiceType = 'VOZ_PERGUNTA'): string {
  if (version === 'V2' && voiceType === 'VOZ_NARRATIVA') {
    return `/audio/prerecorded/v2/${key.toLowerCase()}.mp3`;
  }
  return `/audio/prerecorded/${key.toLowerCase()}.mp3`;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate V2 narrative MP3s with somber voice</name>
  <files>public/audio/prerecorded/v2/*.mp3</files>
  <read_first>
    - scripts/generate-audio-v3.ts (verify --v2 mode is implemented from Plan 39-01)
    - .env.local (verify ELEVENLABS_VOICE_ID_V2 is set correctly)
  </read_first>
  <action>
    1. First, run a dry-run to verify the script is ready:
       ```
       npx tsx scripts/generate-audio-v3.ts --v2 --dry-run
       ```
       Confirm output shows 47 narrative keys marked as GEN, with the v2/ output directory.

    2. Run the actual V2 generation:
       ```
       npx tsx scripts/generate-audio-v3.ts --v2
       ```
       This will:
       - Filter to 47 narrative keys (using getVoiceType)
       - Use ELEVENLABS_VOICE_ID_V2 (GIuLCSVfgJaUuh7hYOY8) for the somber voice
       - Output to public/audio/prerecorded/v2/
       - Apply 1s delay between API calls
       - Skip any files that already exist (resume-safe)

    3. If the script stops due to rate limiting (429 error or quota message), wait 60 seconds and re-run. The script will skip already-generated files.

    4. After generation completes, verify the file count:
       ```
       dir /b public\audio\prerecorded\v2\*.mp3 | find /c ".mp3"
       ```
       Expected: 47 files

    NOTE: This task makes real API calls to ElevenLabs. It will consume approximately 6,900 characters (~7% of Creator plan monthly quota). Estimated cost is negligible. Generation takes approximately 2-3 minutes.
  </action>
  <verify>
    <automated>powershell -Command "(Get-ChildItem 'public/audio/prerecorded/v2/*.mp3').Count"</automated>
  </verify>
  <acceptance_criteria>
    - public/audio/prerecorded/v2/ contains exactly 47 MP3 files
    - All 47 files are non-empty (each > 10 KB)
    - File names match lowercase script keys: inferno_intro.mp3, q1_setup.mp3, q1_resposta_a.mp3, etc.
    - No V2 MP3 exists for VOZ_PERGUNTA keys (no apresentacao.mp3, encerramento.mp3, fallback_*.mp3, timeout_*.mp3, or *_pergunta.mp3 in v2/)
  </acceptance_criteria>
  <done>47 V2 narrative MP3 files generated with somber voice in public/audio/prerecorded/v2/</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate ENCERRAMENTO MP3 with "faca" text fix for V1</name>
  <files>public/audio/prerecorded/encerramento.mp3</files>
  <read_first>
    - src/data/script.ts (verify ENCERRAMENTO text says "Faca alguma coisa com isso." around line 624)
    - public/audio/prerecorded/encerramento.mp3 (confirm the file exists and will be overwritten)
  </read_first>
  <action>
    The ENCERRAMENTO script key text was updated from "Faz alguma coisa com isso." to "Faca alguma coisa com isso." in script.ts (committed in Plan 39-01). The existing encerramento.mp3 still has the old "Faz" audio. Regenerate it.

    ENCERRAMENTO is classified as VOZ_PERGUNTA by getVoiceType(), so:
    - In V2, it uses the V1 voice (shared MP3 from root directory)
    - Only the V1 MP3 needs regeneration
    - No V2 ENCERRAMENTO MP3 is needed

    To regenerate just ENCERRAMENTO without regenerating all 82 files, the simplest approach given the script's current design is:

    Option A (preferred if script supports it): If the extended script allows specifying a single key, use that.

    Option B (safe fallback): Since the script skips existing files and only regenerates with --force, and --force regenerates ALL files, we need a targeted approach. Delete only encerramento.mp3, then run without --force:
    ```
    del public\audio\prerecorded\encerramento.mp3
    npx tsx scripts/generate-audio-v3.ts
    ```
    The script will skip all 81 existing files and only generate the missing encerramento.mp3.

    Use Option B. Steps:
    1. Delete `public/audio/prerecorded/encerramento.mp3`
    2. Run `npx tsx scripts/generate-audio-v3.ts` (V1 mode, no flags)
    3. Script will skip 81 existing files, generate 1 new encerramento.mp3
    4. Verify the file was created and has a reasonable size (> 10 KB)

    NOTE: This makes 1 API call (~205 characters). Negligible cost.
  </action>
  <verify>
    <automated>powershell -Command "if (Test-Path 'public/audio/prerecorded/encerramento.mp3') { (Get-Item 'public/audio/prerecorded/encerramento.mp3').Length -gt 10000 } else { $false }"</automated>
  </verify>
  <acceptance_criteria>
    - public/audio/prerecorded/encerramento.mp3 exists and is > 10 KB
    - File modification timestamp is from the current generation run (newer than other V1 MP3s)
    - Total V1 MP3 count remains 82 (no files added or removed beyond the regeneration)
  </acceptance_criteria>
  <done>ENCERRAMENTO MP3 regenerated with corrected "Faca" text using V1 Oracle voice</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify MP3 coverage and FallbackTTS routing</name>
  <what-built>
    Generated 47 V2 narrative MP3s with somber voice and regenerated 1 V1 ENCERRAMENTO MP3 with "faca" text fix. Total MP3 count should be 129 (82 V1 + 47 V2).
  </what-built>
  <how-to-verify>
    1. Verify file counts:
       - Run: `powershell -Command "(Get-ChildItem 'public/audio/prerecorded/*.mp3').Count"` -> expect 82
       - Run: `powershell -Command "(Get-ChildItem 'public/audio/prerecorded/v2/*.mp3').Count"` -> expect 47
       - Total: 129 MP3 files

    2. Verify V2 directory contains ONLY narrative keys (no pergunta keys):
       - Run: `dir public\audio\prerecorded\v2\*.mp3 /b`
       - Should NOT contain: apresentacao.mp3, encerramento.mp3, any fallback_*.mp3, any timeout_*.mp3, any *_pergunta.mp3
       - SHOULD contain: inferno_intro.mp3, q1_setup.mp3, q1_resposta_a.mp3, devolucao_seeker.mp3, etc.

    3. Run existing tests to verify no regressions:
       - `npx vitest run src/services/tts/__tests__/fallback-tts.test.ts -x`
       - `npx vitest run src/types/__tests__/voice-classification.test.ts -x`

    4. (Optional) Spot-check a V2 MP3: play one of the narrative files (e.g., `public/audio/prerecorded/v2/inferno_intro.mp3`) and confirm it uses the somber voice, not the original Oracle voice.

    5. (Optional) Spot-check the regenerated encerramento.mp3: play `public/audio/prerecorded/encerramento.mp3` and listen for "Faca alguma coisa com isso." (not "Faz").
  </how-to-verify>
  <resume-signal>Type "approved" if file counts match (82 V1 + 47 V2 = 129) and tests pass, or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Script -> ElevenLabs API | Script text + API key sent over HTTPS |
| Static files -> Browser | MP3s served from public/ directory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-03 | Information Disclosure | ElevenLabs API key in script | accept | Key read from .env.local (gitignored), never embedded in output; same risk as existing 82 V1 generations |
| T-39-04 | Denial of Service | Rate limit exhaustion | accept | Script has 1s delay + quota detection + resume-safe (skips existing); ~7% of monthly quota |
</threat_model>

<verification>
1. `(Get-ChildItem 'public/audio/prerecorded/v2/*.mp3').Count` equals 47
2. `(Get-ChildItem 'public/audio/prerecorded/*.mp3').Count` equals 82
3. V2 directory has no *_pergunta.mp3, no fallback_*.mp3, no timeout_*.mp3, no apresentacao.mp3, no encerramento.mp3
4. encerramento.mp3 exists in V1 root directory with recent modification timestamp
5. `npx vitest run src/services/tts/__tests__/fallback-tts.test.ts -x` passes
6. `npx vitest run src/types/__tests__/voice-classification.test.ts -x` passes
</verification>

<success_criteria>
- 47 V2 narrative MP3 files exist in public/audio/prerecorded/v2/
- 82 V1 MP3 files exist in public/audio/prerecorded/ (no change except ENCERRAMENTO regen)
- Total: 129 MP3 files across both directories
- ENCERRAMENTO MP3 regenerated with corrected "Faca" text
- Existing tests pass (FallbackTTS, voice classification)
- FallbackTTS correctly routes V2 narrative segments to v2/ directory
</success_criteria>

<output>
After completion, create `.planning/phases/39-audio-generation-polish/39-02-SUMMARY.md`
</output>
